{
    "configurations": [
        {
            "type": "firefox",
            "request": "launch",
            "reAttach": true,
            "name": "Start HTTP Server",
            "url": "http://localhost:8080",
            "webRoot": "${workspaceFolder}/out",
            "preLaunchTask": "execute"
        },
    ]
}
